There are four implementations of freq here, two in Go,
one in Python, and the original C code; I've added the
C version simply for convenience, I hope this is "fair
use" of the code. The Makefile has a "test" target that
makes sure the programs agree in their output and times
them for comparison.
